import type { Preview } from '@storybook/sveltekit';

// Global styles will be imported here when design tokens are added
// import '../src/styles/global.css';

const preview: Preview = {};

export default preview;
