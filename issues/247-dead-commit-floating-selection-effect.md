---
title: Dead commitFloatingSelection tool effect bypasses the isDrawing seal
status: done
created: 2026-07-17
---

> *This was generated by AI during triage (found while auditing issue 244).*

## Problem

The drawing-tool effect `commitFloatingSelection` is declared in the `ToolEffect`
union and handled in the session's effect applier, but **no tool ever emits it**.
It is a dead branch.

It is worth more than a routine dead-code cleanup because of *what* the branch
does. The handler commits the floating selection directly, whereas the
supported commit path goes through the lifecycle's `commitIfPending`, which
refuses while a stroke is in progress (`getIsDrawing`). So if some future tool
started emitting this effect, it would commit a floating selection mid-stroke —
bypassing a seal the rest of the system relies on, including the precondition
that undo/redo and baseline capture are never re-entered while a stroke is live
(debug-asserted in the core History ring).

## Solution

Confirm the effect is genuinely unused, then either delete it (union member and
handler branch) or, if a caller is actually intended, route it through the
guarded commit path so the `isDrawing` seal holds.

Deleting is the likely answer: the effect union is the tools' vocabulary, and
carrying a member no tool speaks invites someone to reach for the unguarded
path precisely because it looks supported.

## Notes

- Found while tracing whether any command commits while another Edit Baseline
  is pending (it does not — this dead branch was the only candidate).
- No user-visible symptom today; this is latent-hazard cleanup.

## Triage (2026-07-17)

> *This was generated by AI during triage.*

Confirmed dead, and the cause is in the history rather than an unfinished plan.

- **No emitter exists.** The union's siblings (`beginFloatingSelection`,
  `moveFloatingSelection`, `cancelFloatingSelection`) are all emitted by the
  selection tool; `commitFloatingSelection` is emitted by nothing in production
  or test code. `EditorEffects` is an alias of `readonly ToolEffect[]`, so the
  union is exclusively the tools' vocabulary — no other producer can inject it.
- **It was deliberately orphaned.** [142 — Selection drag-to-move](142-selection-drag-to-move.md)
  introduced the member with a real emitter: the selection tool committed on
  drag release in `liftAndDrag` mode. One day later
  [150 — Selection Action Bar Floating state](150-selection-action-bar-floating-state.md)
  decided a drag release must *not* implicitly commit — the Floating stays live
  until the action bar's Done — and replaced the emission with `NO_EFFECTS`.
  The union member and handler branch were left behind. No caller is planned.
- **The hazard framing holds.** The dead branch calls the lifecycle's raw
  `commit()`. The supported path — the tab's public `commitFloatingSelection`,
  which the action bar's Done drives — goes through `commitIfPending()` and
  refuses while `getIsDrawing()` is true. Deleting the branch removes the only
  *external* unguarded entry; `commit()` itself stays, since the lifecycle still
  calls it internally (paste, duplicate, draw-start policy, drag-starts-outside).
- Web shell only — neither the Rust core nor the Apple shell mirrors this union.

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Delete the unemitted `commitFloatingSelection` tool effect so the
tools' effect vocabulary carries no unguarded commit path.

**Current behavior:**
`ToolEffect` — the union describing what drawing tools report back to the editor
session — declares a `commitFloatingSelection` member, and the session's effect
applier handles it by committing the Floating Selection through the lifecycle's
unguarded `commit()`. No tool emits it, so the branch never runs. Because the
member looks supported, a future tool could emit it and commit a Floating
Selection mid-stroke, bypassing the `isDrawing` seal that `commitIfPending()`
enforces and violating the precondition that the core History ring debug-asserts
(no baseline capture or undo/redo re-entry while a stroke is live).

**Desired behavior:**
The effect no longer exists. Tools cannot express "commit the Floating Selection"
at all; the only commit paths are the tab's public commit entry point (guarded by
`commitIfPending()`) and the lifecycle's own internal callers. All existing
Floating Selection behavior — lift, move, cancel, drag release leaving the
Floating live, action-bar Done/Cancel/Duplicate, paste, nudge — is unchanged.

**Key interfaces:**
- `ToolEffect` — drop the `commitFloatingSelection` member. The other Floating
  members stay; they have live emitters.
- The editor session's effect applier — drop the matching branch. It exhausts the
  union via an `assertNever` default, so the type checker flags any miss.
- `FloatingSelectionLifecycle.commit()` — keep. Still called internally.
  `commitIfPending()` remains the sole guarded external commit.

**Acceptance criteria:**
- [ ] No `commitFloatingSelection` member remains in the tool effect union, and
      no handler branch for it remains in the effect applier.
- [ ] `bun run check` passes — exhaustiveness holds with no `assertNever` error.
- [ ] The full web test suite passes unchanged; no test needed modification to
      accommodate the deletion.
- [ ] Manual or test verification that the action bar's Done still commits a
      Floating Selection and that drag release still leaves it live.

**Out of scope:**
- Changing when or how a Floating Selection commits — the 150 decision (drag
  release does not implicitly commit) stands.
- Refactoring `FloatingSelectionLifecycle`, the `isDrawing` seal, or the core
  History ring's assertions.
- The other Floating effect members, the Apple shell, and the Rust core.

## Results

| File | Description |
|------|-------------|
| `src/lib/canvas/draw-tool.ts` | Dropped the `commitFloatingSelection` member from the `ToolEffect` union, so tools can no longer express "commit the Floating Selection". |
| `src/lib/canvas/editor-session/tab-state.svelte.ts` | Removed the matching effect-applier branch, the only external caller of the lifecycle's unguarded `commit()`. |

### Key Decisions

- **Deleted rather than routed through the guarded path.** The brief's two
  options were delete or wire the effect to `commitIfPending()`. Deletion won on
  evidence: git history shows the member was orphaned by a deliberate design
  decision ([150](150-selection-action-bar-floating-state.md) — drag release must
  not implicitly commit), not by an unfinished plan, so no caller is intended.
  Keeping a member no tool speaks would only advertise the unguarded path.
- **No new tests written.** Every behavior the deletion could break was already
  pinned: the selection tool's drag release emitting nothing (`session.end()` →
  `[]`), cancel emitting no commit effect, `commitIfPending` refusing mid-stroke,
  and the action bar's Done commit with undo. A test for a branch being deleted
  would be a dead test for dead code. The deletion's proof is that the suite
  passed **unmodified** — a live branch would have broken something.
- **`FloatingSelectionLifecycle.commit()` kept.** Still called internally by
  paste, duplicate, the draw-start policy, and the drag-starts-outside path.
  Only the external unguarded entry point is gone.

### Notes

- The `isDrawing` seal is now enforced by construction on the tool seam: with the
  effect gone, `commitIfPending()` is the sole external commit path, so no tool
  can commit a Floating Selection mid-stroke and violate the precondition the
  core History ring debug-asserts. This is the tool-effect counterpart to what
  [246](246-remove-eager-push-api.md) did for history recording.
- Web shell only — neither the Rust core nor the Apple shell mirrors this union,
  so `docs/platform-status.md` needs no change (no feature row is affected).
- Verified with `bun run check` (0 errors), the full unit suite (1624 tests, no
  test file modified), and `e2e/editor/selection.test.ts` (Floating commit +
  undo, Escape cancel).
