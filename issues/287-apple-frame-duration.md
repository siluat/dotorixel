---
title: Apple per-frame duration — timing editor on the timeline
status: ready-for-agent
created: 2026-08-16
---

> *This issue was generated by AI from the parent RFC.*

## Parent

[013 — Apple native catch-up — phased strategy to reach full web parity](013-apple-native-catchup.md)
(Phase 6 — Animation + extended export)

## What to build

Per-frame display-duration editing in the Timeline panel (web parity
with the duration timeline UI), so a user can time holds and quick
passes frame by frame:

- **A duration editor for the active frame**, in milliseconds, using a
  draft-commit pattern: the field holds raw in-progress text while
  editing and clamps to the binding-owned 1–60 000 ms range on commit
  (the same bounds the 283 binding exposes).
- **A derived fps read-out** (1000 / ms) beside the editor — display
  only, never an input; per-frame ms stays the single source of truth.
- Committing a changed duration is **undoable** through the
  whole-document snapshot history; committing an unchanged value pushes
  no history entry.
- Durations are per-frame: retiming one frame leaves the others
  untouched, and a duplicated frame carries its source's duration (core
  behavior — verify at the shell seam).

## Acceptance criteria

- Editing the active frame's duration and committing updates that
  frame's stored duration; switching frames shows each frame's own
  value.
- Out-of-range and non-numeric input clamps/reverts on commit rather
  than corrupting the value.
- A committed change is undoable; an unchanged commit creates no
  history entry.
- The fps read-out tracks the committed duration.
- Touch targets ≥ 44 pt; new labels in the String Catalog (en/ko/ja);
  snapshot baselines updated.

## Blocked by

- [284 — Apple frame ruler](284-apple-frame-ruler.md)
  (parallel with [285](285-apple-frame-operations.md))
