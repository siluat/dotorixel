pre-commit:
  commands:
    check:
      run: bun run check
    markdownlint:
      glob: "*.md"
      run: bunx markdownlint-cli2 {staged_files}
    rustfmt:
      glob: "*.rs"
      run: cargo fmt --check
