# Progress

## Currently Working On

Reference Layer type — tracing reference for pixel artwork ([PRD](../issues/105-reference-layer-type.md)). 7/20 sub-issues done; the last Rust-only sampling slice is complete, so 113 (WASM facade + TS interface) is now unblocked.

## Last Completed

[112 — Rust core sampling-aware active-layer accessor](../issues/112-reference-layer-document-try-get-pixel.md). Preserves strict Pixel Layer reads while enabling optional Reference Layer sampling for follow-up shell work.

## Next Up

- 113 — Reference Layer: WASM facade + TS `canvas-model.ts` interface
- Apple Phase 1 — Responsive tiers (iPad compact / iPad regular / Mac)
- Apple Phase 1 — Enable clear canvas (existing disabled button)
- Apple Phase 1 — Enable PNG export (existing disabled button)
- Apple Phase 1 — Shift-constrain for shape tools (on hold; shape tools not on Apple yet)
