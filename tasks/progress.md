# Progress

## Currently Working On

None

## Last Completed

Alt key temporary eyedropper tool switch

## Next Up

- Modifier key: Space=pan (temporary pan mode)
  - No dependencies. Pan infrastructure already exists. Same pattern as Alt eyedropper.
- Edit shortcuts (Ctrl+Z/Y, X=swap colors)
  - No dependencies. Undo/redo and swap already exist; this adds keyboard bindings.
- On-demand shortcut hint badges (show on key hold, toggle off in settings)
  - Depends on all modifier key shortcuts being implemented first (Alt, Space, Shift).
