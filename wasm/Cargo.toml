[package]
name = "dotorixel-wasm"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.114"
dotorixel-core = { path = "../crates/core" }
uuid = "1"

[dev-dependencies]
uuid = { version = "1", features = ["v4"] }
